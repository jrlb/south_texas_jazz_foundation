
- @biographies.sort_by { |b| b.last_name }.each do |biography|
  .row
    .span4= link_to biography.first_name + ' ' + biography.last_name, biography
    - if member_signed_in?
      .span2= link_to 'Edit', edit_biography_path(biography)
      .span2= link_to 'Destroy', biography, method: :delete, data: { confirm: 'Are you sure?' }
%br 
