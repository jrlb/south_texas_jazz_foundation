%p
  Released
  = time_ago_in_words @press_release.created_at
  ago
%p
  = @press_release.story

- if member_signed_in?
  = link_to 'Edit', edit_press_release_path(@press_release)
  \|
  = link_to 'Back', press_releases_path
